# Optional, production only: Backend API base URL (no trailing slash).
# In dev we always use Vite proxy (/api -> backend) to avoid CORS.
# Set in production (e.g. Netlify env vars) if /api proxy returns 404:
# VITE_API_BASE=https://www.itcatalystindia.com/Development/CustomerPortal_API
